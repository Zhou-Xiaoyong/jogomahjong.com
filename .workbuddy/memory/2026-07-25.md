# 2026-07-25

## 长尾内容更新体系（用户新需求）
用户要求：基于网站特点与 SEO，制定 100+ 长尾词库 CSV，之后按词库顺序每周 1 篇更新 Culture / How to play 栏目，做好双向内链（老页→新文、新文→相关页），并制定自动更新计划。

### 已完成
1. **长尾词库 `content/keywords.csv`**：108 个关键词（Culture 54 + HowTo 54），按 C,C,H,H 交错排布覆盖均衡。每词含 keyword_pt/en、category、section、slug_pt/en、intent、priority（Alta16/Média43/Baixa49）、link_to、link_from、status、published_date、notes。link_to/link_from 为站内既有 URL（游戏页 + hub），保证内链可落地。
2. **文章模板**：`content/article-template.html`（PT）、`content/article-template-en.html`（EN），含完整 head（canonical + hreflang 三向 + Article JSON-LD）、topbar/footer、content-grid/sidebox 布局，复用站点 CSS 类。
3. **接线脚本 `content/update_indexes.py`**（幂等）：把新文写入 4 个 hub 的 `<ul id="article-index">`、追加到 sitemap.xml + en/sitemap.xml（带 xhtml:link 跨语言互指）、向 link_from 游戏页注入内链、回写 CSV 状态为 Gerado。
4. **4 个 hub 页**（`cultura-mahjong/`、`como-jogar-mahjong/` 及 EN 对应）插入「Índice de artigos」`<ul id="article-index">` 容器。
5. **示范文章 seq=1**：`cultura-mahjong/historia-do-mahjong/`（PT+EN），已跑通整条管线（本地服务器验证 200、JSON 合法、无残留占位符、hub 索引与 sitemap 已写入）。CSV 标记 Gerado。
6. **自动化任务 `automation-1784994289655`**：每周一 ACTIVE，读 CSV→取首个 Pendente→写 PT+EN 文章→跑 update_indexes.py→**不推送**（等用户确认）。

### 修复的 bug
- EN sitemap/hreflang URL 重复 `/en/`（`en.jogomahjong.com/en/culture-mahjong/...`）→ 修正 CONFIG 的 art_en_dir（去掉 en/ 前缀），并修补两个 sitemap。

### 待用户确认/后续
- 全部为本地未提交改动。等用户检查后一并 commit/push（含 7 月遗留的 hreflang/sitemap/首页数量词修复）。
- 可选：未来若信任自动产出，可把部署步骤也自动化（但需先确认 Cloudflare Pages 发布方式）。
